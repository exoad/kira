module "app:main"

use "app:ranges"
use "app:checks"

fx main(): Void {
    mut i: Int32 = 0
    while i < 2 {
        i = i + 1
    }

    value: Int32 = sumTo(5)
    label: Str = parityLabel(value)
    trace(label)
}
