module "app:main"

fx main(): Void {
    numbers: Arr<Int32> = [1, 2, 3]
    head: Int32 = numbers[0]
    bag: Map<Str, Int32> = Map<Str, Int32> { }
    if bag.isEmpty() {
        trace(head)
    }
}
